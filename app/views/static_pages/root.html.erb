<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>
  window.images = {
    logo: "<%= image_path("logo.png") %>",
    missing_artwork: "<%= image_path("default.png") %>",
    nixon: "<%= image_path("nixon.jpg") %>"
  }
</script>

<div id="root"></div>
